# Audit index

The current-release numerical lock is in `../RELEASE_MANIFEST.json`. The
figure source tables are the smallest public, auditable representations of the
reported aggregates. Full CSD entry/site tables and any internal licensed
identifiers remain on the author's licensed workstation and are intentionally
not redistributed.

For a fresh run, use the frozen protocols in `../protocols/` and the scripts in
`../models/E3_baselines/`. A valid CCDC installation is required for CSD
reproduction. The current periodic result is a local-state audit, not a claim
of complete MOF-framework identity or universal property-prediction
superiority.
